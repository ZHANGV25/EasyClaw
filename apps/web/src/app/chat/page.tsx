"use client";

import ChatView from "@/components/ChatView";

/**
 * /chat — New conversation (no ID).
 * Shows the welcome message and allows the user to start a new chat.
 */
export default function ChatPage() {
  return <ChatView />;
}
